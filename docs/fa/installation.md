# نصب

## پیش‌نیازها

- پایتون 3.9+
- pandas >= 2.0
- numpy >= 1.23
- jdatetime >= 4.0

## نصب با pip

```bash
pip install -U jalali-pandas
```

## نصب با uv

```bash
uv pip install jalali-pandas
```

## افزونه‌های اختیاری (برای مشارکت)

```bash
uv sync --extra dev
uv sync --extra docs
```
